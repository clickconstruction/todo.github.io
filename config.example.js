// Copy this file to config.js and add your Supabase credentials.
// NOTE: config.js is COMMITTED in this repo (static deploy; the publishable key
// is a public, RLS-gated credential — never put a service-role/secret key or
// password in it). Dev-only overrides belong in config.local.js, which IS
// gitignored and loaded on localhost only.

window.SUPABASE_URL = 'https://your-project.supabase.co';
window.SUPABASE_ANON_KEY = 'your-publishable-key';
